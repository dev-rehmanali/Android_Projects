<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Appproject">
        <activity android:name=".reservation"></activity>
        <activity android:name=".newreservation" /> <!-- <activity android:name=".MainActivity"> -->
        <activity android:name=".creditcardpayment" />
        <activity android:name=".donation1" />
        <activity android:name=".jazzcashpayment" />
        <activity android:name=".otpverification" />
        <activity android:name=".resetpassword" />
        <activity android:name=".setnewpassword" />
        <activity android:name=".SignUp1" />
        <activity android:name=".Login1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>